spring:
  application:
    name: face-zuul

server:
  port: 8768

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

zuul:
  routes:
    api-a:
      path: /api/staff/**
      serviceId: face-web-staff-feign
